Notas:
Tablas que van a Cambiar:

Insertar Reservacion:
	idReservacion 			(NULL)
	fechaReservacion		(Curdate())
	fechaEntrada				(fechaEntrada(Actual) > fechaSalida(Previa)) - validacion
	fechaSalida					
	camaSupletoria
	estado							(Activo/Reservado)
	observacion
	idCliente
		
		Insertar Cliente si no Existe
			idCliente
			fechaRegistro
			estado
			idPersona

Insertar Factura:
	idFactura 					(NULL)
	numFactura
	fechaEmision				(Curdate())
	costeReservacion
	costePedido
	costeProducto
	costeTotal
	cambio
	observacion
	idCliente
	idEmpleado
	idTipoFactura
	idModoPago

Insertar DetalleFactura:
	idDetalleFactura 		(NULL)
	cantidad
	descripcionReser
	idFactura
	idProducto
	idPedido
	idReservacion


Mostrar Habitaciones disponibles en determinada sucursal - Listo
Cambiar el Estado de la habiatacion ocupada a Ocupada luego de la Reservacion

